version: "3.9"

services:

  pyecca:
    environment:
      - DISPLAY=$DISPLAY
    container_name: pyecca
    image: ${REGISTRY}/cognipilot/pyecca:${TAG}
    build:
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    privileged: true
    hostname: pyecca
    volumes:
      - ./workdir:/workdir
      - /tmp/.X11-unix:/tmp/.X11-unix
